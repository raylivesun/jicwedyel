package java.procedure;

import java.security.cert.CertPath;
import javax.xml.crypto.Data;

public class getApplyMethod {

    private static Object humanity;

    @SuppressWarnings("unlikely-arg-type")
    public static void certSecurityData(CertPath stream, SecurityException money, Data moveData) throws Exception {
        if (moveData != null) {
            moveData.equals(moveData);
            moveData.getClass();
            moveData.hashCode();
            moveData.notify();
            if (money != lives(humanity)) {
                money.addSuppressed(money);
                money.equals(moveData);
                money.getCause();
                money.getMessage();
                money.hashCode();
                money.notify();
                for(int i = 0; i < 8512*100; i++) {
                    humanity(i++);
                }
                
            } else if (moveData != null) {
                moveData.equals(moveData);
                moveData.getClass();
                moveData.notify();
                humanity(851200*5000);
            }
        } 
    }

    private static void humanity(int i) {
        // Auto-generated method stub
        throw new UnsupportedOperationException("Unimplemented method 'humanity'");
    }

    private static SecurityException lives(Object humanity2) {
        // Auto-generated method stub
        throw new UnsupportedOperationException("Unimplemented method 'lives'");
    }

    
}